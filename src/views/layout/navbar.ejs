<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
   <div class="container-fluid">
      <a class="navbar-brand" href="/">Funix-ASM2</a>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
               <a class="nav-link <%= path === '/' ? 'active' : '' %>" aria-current="page" href="/">Home</a>
            </li>
            <% if (isAuth) { %>
               <li class="nav-item">
                  <a class="nav-link <%= path === '/user-info' ? 'active' : '' %>" aria-current="page" href="/user-info">Trang Cá Nhân</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link <%= path === '/working-info' ? 'active' : '' %>" aria-current="page" href="/working-info">Thông Tin Giờ Làm</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link <%= path === '/user-covid-info' ? 'active' : '' %>" aria-current="page" href="/user-covid-info">Thông Tin Covid Cá Nhân</a>
               </li>
            <% } %>
            <% if (isAuth && user.accountType === 'admin') { %>
               <li class="nav-item">
                  <a class="nav-link <%= path === '/manage' ? 'active' : '' %>" aria-current="page" href="/manage">Trang Quản Lý</a>
               </li>
            <% } %>
         </ul>
      </div>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
         <% if (!isAuth) { %>
            <li class="nav-item">
               <a class="nav-link <%= path === '/login' ? 'active' : '' %>" aria-current="page" href="/login">Đăng Nhập</a>
            </li>
         <% } else { %>
            <li class="nav-item">
               <form action="/logout" method="post">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button class="nav-link" style="background-color: transparent; border-color: transparent;" type="submit">Đăng Xuất</button>
               </form>
            </li>
         <% } %>
     </ul>
   </div>
</nav>
